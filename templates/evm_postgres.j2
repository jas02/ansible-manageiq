#!/bin/bash
# Description: Sets the PostgreSQL environment for scripts and console users
#

export APPLIANCE_PG_CTL=/usr/pgsql-{{ manageiq_postgresql_version }}/bin/pg_ctl
export APPLIANCE_PG_MOUNT_POINT=/var/lib/pgsql/{{ manageiq_postgresql_version }}
export APPLIANCE_PG_DATA=${APPLIANCE_PG_MOUNT_POINT}/data
export APPLIANCE_PG_SERVICE=postgresql-{{ manageiq_postgresql_version }}
export APPLIANCE_PG_SCL_NAME=postgresql-{{ manageiq_postgresql_version }}
export APPLIANCE_PG_PACKAGE_NAME=postgresql{{ manageiq_postgresql_version_short }}-server

#[[ -s /opt/rh/${APPLIANCE_PG_SCL_NAME}/enable ]] && source /opt/rh/${APPLIANCE_PPG_SCL_NAME}/enable
