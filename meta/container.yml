# Add your Ansible Container service definitions here.
# For example:
  #
  # web:
  #   image: ubuntu:trusty
  #   ports:
  #     - "80:80"
  #   command: ['/usr/bin/dumb-init', '/usr/sbin/apache2ctl', '-D', 'FOREGROUND']
  #   dev_overrides:
  #     environment:
  #       - "DEBUG=1"
manageiq:
  from: 'manageiq/manageiq-pods:frontend-latest-gaprindashvili'
  command: ['/usr/bin/dumb-init', 'bash']
